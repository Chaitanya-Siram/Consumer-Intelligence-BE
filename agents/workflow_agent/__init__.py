"""Conversational workflow-builder agent.

Turns a free-form research request into a validated workflow graph (the same
nodes + edges the designer produces), asking for whatever the request leaves
missing and confirming the boolean query before it builds.
"""
