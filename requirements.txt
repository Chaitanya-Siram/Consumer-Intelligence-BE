fastapi==0.115.5
uvicorn[standard]==0.32.1
python-multipart==0.0.17
pandas==2.2.3
openpyxl==3.1.5
anthropic
openai>=1.40.0
python-dotenv==1.0.1
pydantic==2.9.2
email-validator==2.3.0
bcrypt==4.2.1
cryptography>=42.0.0
PyJWT==2.10.1
psycopg2==2.9.9
pgvector==0.3.6
boto3
requests
SQLAlchemy==2.0.27
# RAG: LlamaIndex + local BGE embeddings/reranker + LangGraph retrieval agent
llama-index-core==0.12.10
llama-index-vector-stores-postgres==0.4.1
llama-index-embeddings-huggingface==0.4.0
llama-index-embeddings-nvidia==0.3.4
sentence-transformers==3.3.1
llama-index-llms-azure-openai==0.3.0
llama-index-llms-anthropic==0.6.4
langgraph==0.2.60
e2b-code-interpreter==2.9.0
serpapi>=0.1.5
feedparser==6.0.12
newspaper3k==0.2.8
beautifulsoup4==4.15.0
lxml-html-clean==0.4.5
python-docx==1.2.0
tzdata==2024.1
tavily-python
# Social Research "Supporting Verbatims": server-side screenshot capture for
# posts with no public embed API. Needs `playwright install chromium` once
# after installing requirements (not run automatically by pip).
playwright==1.49.0
# CI storyboard banners: keyless DuckDuckGo image search, tried before Pexels
# for every card photo (see brand_media.duckduckgo_image) — MCP servers such
# as the ddg-search one configured for this project are Claude Code's own
# tools and are not reachable from this running server, so the backend talks
# to DuckDuckGo directly instead.
ddgs==9.16.0